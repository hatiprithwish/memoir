import { ClerkExpressWithAuth } from "@clerk/clerk-sdk-node";
import { Router } from "express";

const router = Router();

// router.route("/")
// .get(ClerkExpressWithAuth(), getNotesByUser)
// .post(ClerkExpressWithAuth(), createNewNote)

export default router;
